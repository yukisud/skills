---
query: "our wire format has pack() and unpack() in src/codec.py and i keep finding weird bugs where a message comes back different. can you write tests that would actually catch that class of thing"
should_trigger: true
---
